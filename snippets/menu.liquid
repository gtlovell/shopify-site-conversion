<nav>
  {% for link in linklists[section.settings.main_linklist].links %}
    {% if link.links == blank %}
      <li>
        <a href="{{link.url}}" class="nav-hover">{{link.title}}</a>
      </li>
    {% endif %}
  {% endfor %}
  {% comment %} <li>
    <a href="#work" class="nav-hover">Work</a>
  </li>
  <li>
    <a href="#about" class="nav-hover">About</a>
  </li>
  <li>
    <a href="#contact" class="nav-hover">Contact</a>
  </li> {% endcomment %}
</nav>